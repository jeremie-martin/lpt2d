"""Filenames inside a render bundle. Single source of truth for the layout."""

from __future__ import annotations

VIDEO = "video.mp4"
PARAMS = "params.json"
VERDICT = "verdict.json"
FRAME_SHOT = "frame.shot.json"
OVERRIDES = "publish.json"

MARKER_UPLOADED = "uploaded"
MARKER_FAILED = "failed"
