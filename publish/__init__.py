"""Publish pipeline: process lpt2d renders and upload to YouTube.

See `publish.cli` for the command-line entry point. Run via:

    uv run python -m publish process INPUT.mp4
    uv run python -m publish upload VIDEO.mp4
    uv run python -m publish publish INPUT.mp4
    uv run python -m publish watch
"""
